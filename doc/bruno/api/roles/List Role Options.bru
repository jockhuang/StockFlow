meta {
  name: List Role Options
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/roles/options
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
}
