meta {
  name: Update Resource
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/resources/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "code": "AUTH_PROFILE_READ",
    "name": "Current user",
    "path": "/api/auth/me",
    "httpMethod": "GET",
    "description": "Read authenticated user profile"
  }
}
